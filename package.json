{
  "name": "ortools-cpsat-wasm",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/wasm/cp_sat_runtime.js",
  "types": "dist/wasm/cp_sat_runtime.d.ts",
  "scripts": {
    "build:wasm": "bash -lc 'scripts/ensure-emsdk.sh && source ./emsdk/emsdk_env.sh && cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=$EMSDK/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake && cmake --build build --target cp_sat_runtime_site -j'",
    "build:lib": "vite build --config vite.lib.config.ts",
    "build:site": "vite build --config vite.site.config.ts",
    "build": "npm run build:wasm && npm run build:site && npm run build:lib",
    "pack:lib": "npm run build:lib && npm pack --pack-destination build/javascript/lib",
    "clean": "rm -rf build",
    "package": "npm run build && npm pack",
    "dev": "vite --config vite.site.config.ts dev --host",
    "start": "npm run dev",
    "preview": "vite --config vite.site.config.ts preview --host"
  },
  "files": [
    "dist/wasm",
    "dist/site"
  ],
  "optionalDependencies": {
    "protobufjs": "^7.5.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}
